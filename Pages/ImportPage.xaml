<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="IntuneTools.Pages.ImportPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:IntuneTools.Pages"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:controls="using:CommunityToolkit.WinUI.UI.Controls"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Grid RowDefinitions="Auto,Auto,*,Auto" Margin="20">
        <!-- Page Title -->
        <TextBlock Text="Import Page" 
                   Grid.Row="0"
                   HorizontalAlignment="Center" 
                   FontSize="28" 
                   FontWeight="Bold"
                   Margin="0,0,0,20"/>
        <!-- Login Button -->
        <Button x:Name="Import" 
                Content="Login" 
                Width="120" 
                HorizontalAlignment="Center" 
                Grid.Row="1"
                Margin="0,0,0,20" 
                Click="ImportButton_Click"/>
        <!-- Data Grid -->
        <controls:DataGrid x:Name="ContentDataGrid" 
                           Grid.Row="2"
                           AutoGenerateColumns="False" 
                           Margin="0,0,0,20"
                           Height="300">
            <controls:DataGrid.Columns>
                <controls:DataGridTextColumn Header="ContentName" Binding="{Binding ContentName}" Width="*"/>
                <controls:DataGridTextColumn Header="ContentID" Binding="{Binding ContentID}" Width="*"/>
                <controls:DataGridTextColumn Header="ContentType" Binding="{Binding ContentType}" Width="*"/>
            </controls:DataGrid.Columns>
        </controls:DataGrid>
        <!-- Info/Status Block -->
        <RichTextBlock Grid.Row="3" Margin="0,10,0,0">
            <Paragraph>Test</Paragraph>
        </RichTextBlock>
    </Grid>
</Page>
