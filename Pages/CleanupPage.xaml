<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="IntuneTools.Pages.CleanupPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:IntuneTools.Pages"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:controls="using:CommunityToolkit.WinUI.UI.Controls"
    mc:Ignorable="d">

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="500"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <StackPanel Grid.Column="0" Grid.RowSpan="3" Margin="0,10,10,10">
            <!-- Add your controls here -->
        </StackPanel>
        <StackPanel Grid.Column="1" Grid.Row="0" Orientation="Horizontal" Margin="0,10,10,0" MinHeight="40" Spacing="10">
            <Button x:Name="SearchButton" Content="Search" />
            <Button x:Name="ListAllButton" Content="List All" />
            <Button x:Name="DeleteButton" Content="Delete" />
        </StackPanel>
        <TextBox x:Name="InputTextBox" Grid.Column="1" Grid.Row="1" Margin="0,5,10,5" PlaceholderText="Enter text here..." />
        <controls:DataGrid x:Name="CleanupDataGrid" AutoGenerateColumns="False" Margin="0,10,10,10" Grid.Column="1" Grid.Row="2">
            <controls:DataGrid.Columns>
                <controls:DataGridTextColumn Header="Name" Binding="{Binding Name}" Width="*"/>
                <controls:DataGridTextColumn Header="Type" Binding="{Binding Type}" Width="*"/>
                <controls:DataGridTextColumn Header="Platform" Binding="{Binding Platform}" Width="*"/>
                <controls:DataGridTextColumn Header="ID" Binding="{Binding ID}" Width="*"/>
            </controls:DataGrid.Columns>
        </controls:DataGrid>
    </Grid>
</Page>
