<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="IntuneTools.UI.BlankPage1"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:IntuneTools.UI"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:controls="using:CommunityToolkit.WinUI.UI.Controls"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Grid>
        <controls:DataGrid x:Name="dataGrid"
            Height="800" Margin="2" Width="500">

            <controls:DataGrid.Columns>
            <controls:DataGridTextColumn Header="Name" Binding="{Binding Name}" />
            <controls:DataGridTextColumn Header="Age" Binding="{Binding Age}" />
            <controls:DataGridTextColumn Header="Test" Binding="{Binding Test}" />
            </controls:DataGrid.Columns>

        </controls:DataGrid>



        <TextBlock Text="Demo Page" HorizontalAlignment="Center" VerticalAlignment="Center"/>
    </Grid>
</Page>
